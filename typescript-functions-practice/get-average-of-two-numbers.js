'use strict';
/* exported getAverageOfTwoNumbers */
// function getAverageOfTwoNumbers(x: number, y: number): number {
//   return (x + y) / 2;
// }
// const getAverageOfTwoNumbers = (x: number, y: number): number => (x + y) / 2;
function getAverageOfTwoNumbers(x, y) {
  return (x + y) / 2;
}
