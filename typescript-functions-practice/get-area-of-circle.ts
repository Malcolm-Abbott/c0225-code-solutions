/* exported getAreaOfCircle */
// const getAreaOfCircle: Function = (radius: number): number =>
//   Math.PI * radius ** 2;
function getAreaOfCircle(radius: number): number {
  return Math.PI * radius ** 2;
}
